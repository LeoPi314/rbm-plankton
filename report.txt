  Theta trajectories are nearly identical across all NBB-RBM models at around epoch 130. Theta drift is a property of the training procedure, not L-specific. So theta is not the driver or root cause for instabilities.

The instability is in the weights W, not the initial conditions per se - it's a learning rate / landscape curvature mismatch - Evidence:
  - nb_L10 shows larger MSE oscillation amplitude from epoch 40 onward (swings of ±40 vs ±15 for L=7)
  - The tipping point hits around epoch 120 when theta~1.09 — the same theta value that is perfectly stable for L=7
  - This means the weights in L=10 are making progressively larger steps even though theta is identical

L=10 can be made to converge by tweaking learning rate but not worth casue the current behaviour already suggests 10 hidden are not needed:
  1. NLL plateau: L=7 already shows diminishing returns over L=5/6. Adding 3 more hidden units to reach L=10 wouldn't buy meaningful compression of new community structure — the relevant ecological variation is already captured.
  2. Instability as a signal: the growing MSE oscillations from epoch 40 onward aren't just a hyperparameter problem — they indicate the L=10 loss landscape is poorly conditioned for this data. A model with more capacity than the data can support tends to oscillate because multiple near-equivalent weight configurations exist. Forcing
convergence with a lower LR would likely give you redundant or near-zero hidden units anyway.

Ready to dive into the analysis — want to start with the NLL/MSE comparison across L values, or jump straight into the hidden activations to check community structure?







  Memo: Jan-Feb 2023 anomaly — verification needed

  What to look up in Eyring 2025: Check whether the paper documents a high-biomass event in winter 2022–2023 (approximately December 2022
  through February 2023). The data shows total abundance ~7× the dataset mean during that window, dropping abruptly back to baseline by March
  2023.

  Decision criteria:
  - If the paper attributes it to a known ecological event (e.g., a winter bloom, a documented anomalous year) → data is valid, no action
  needed. The model already trained on it. Close the item.
  - If the paper flags it as a measurement/classifier artifact → it would warrant exclusion and retraining. But given the NaN patterns are
  already linked to the ML classifier retraining event (LOG-002), and this window has no NaN rows, an artifact origin seems unlikely.

  Are we missing anything else? No. The study is otherwise complete:
  - L=6 selected and statistically validated
  - Hidden analysis (mean activation, coactivation, cross-model) done
  - NaN test evaluation done
  - Gaussian hidden units explicitly deferred as future work
  - bernoulli_zero confirmed trivial

  If the paper confirms a real ecological event, the anomaly item closes with no code changes, and the study is done.